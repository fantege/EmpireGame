# 项目结构说明

## 目录结构

```
帝国：博弈/
├── .git/                    # Git版本控制目录
├── .lingma/                 # Lingma IDE配置目录
├── main.gd                  # 主场景脚本文件
├── main.gd.uid              # 主场景脚本唯一标识符文件
├── main.tscn                # 主场景文件
├── project.godot            # Godot项目配置文件
├── icon.svg                 # 项目图标文件
├── icon.svg.import          # 项目图标导入配置文件
├── .editorconfig            # 编辑器配置文件
├── .gitattributes           # Git属性配置文件
├── .gitignore               # Git忽略文件配置
├── default_theme.tres       # 默认主题资源文件
├── fonts.ttf                # 项目统一UI字体资源
├── 项目注意事项.md          # 项目注意事项文档
├── 项目结构说明.md          # 项目结构说明文档(当前文件)
├── 场景结构说明.md          # 场景结构说明文档
└── 《帝国：博弈》维多利亚时代国家领导人卡牌游戏设计文档-06e2d691b1.md  # 游戏设计文档
```

## 文件详细信息

### 核心文件

| 文件名 | 路径 | UID | 说明 |
|--------|------|-----|------|
| main.gd | `res://main.gd` | uid://cdsnpjbuhlxwa | 主场景脚本文件 |
| main.tscn | `res://main.tscn` | uid://cxb0omrt2dqwf | 主场景文件 |
| project.godot | `res://project.godot` | 无 | Godot项目配置文件 |
| icon.svg | `res://icon.svg` | uid://wkhh33157l4i (见icon.svg.import) | 项目图标文件 |
| default_theme.tres | `res://default_theme.tres` | uid://bq0qd65osa6xf | 默认主题资源文件 |
| fonts.ttf | `res://fonts.ttf` | 无 | 项目统一UI字体资源 |

### 配置文件

| 文件名 | 路径 | UID | 说明 |
|--------|------|-----|------|
| .editorconfig | `res://.editorconfig` | 无 | 编辑器配置文件 |
| .gitattributes | `res://.gitattributes` | 无 | Git属性配置文件 |
| .gitignore | `res://.gitignore` | 无 | Git忽略文件配置 |

### 文档文件

| 文件名 | 路径 | UID | 说明 |
|--------|------|-----|------|
| 项目注意事项.md | `res://项目注意事项.md` | 无 | 项目注意事项文档 |
| 项目结构说明.md | `res://项目结构说明.md` | 无 | 项目结构说明文档 |
| 场景结构说明.md | `res://场景结构说明.md` | 无 | 场景结构说明文档 |
| 《帝国：博弈》维多利亚时代国家领导人卡牌游戏设计文档-06e2d691b1.md | `res://《帝国：博弈》维多利亚时代国家领导人卡牌游戏设计文档-06e2d691b1.md` | 无 | 游戏设计文档 |

## 场景和节点结构

### 主场景 (main.tscn)

- **场景类型**: Node2D
- **关联脚本**: main.gd (uid://cdsnpjbuhlxwa)
- **节点结构**:
  - Main (Node2D)
    - 挂载脚本: main.gd
    - Background (ColorRect) - 背景颜色
    - GameTitle (Label) - 顶部标题（字体 fonts.ttf，已缩小）
    - CardScrollContainer (ScrollContainer) - 旧卡牌展示容器（当前隐藏）
      - CardContainer (HBoxContainer)
    - DeckPanel (Panel) - 主牌堆面板（4:5比例，120×150）
      - DeckLabel (Label) - 显示“牌堆：N”
    - EventDeckPanel (Panel) - 事件牌堆面板（4:5比例，120×150）
      - EventDeckLabel (Label) - 显示“事件牌堆：M”
    - DiscardPilePanel (Panel) - 弃牌堆面板（4:5比例，120×150）
      - DiscardLabel (Label) - 显示“弃牌堆：K”
    - EventSlots (Node) - 事件槽容器（四个白色边框矩形，顶端横向排列）
      - EventSlotPanel1/2/3/4 (Panel) - 事件槽（4:5比例，120×150，透明背景白边框）
    - LeaderNameContainer (Control) - 领袖名称彩色矩形容器（脚本动态生成）

#### 资源与子资源一致性修复
- 场景内事件槽边框样式的子资源ID由字符串改为数值ID（`id=1`）。
- 所有事件槽面板的样式引用统一为 `SubResource("1")`，避免 Godot 解析器报告 `Invalid parameter` 错误。
- 外部资源 `ExtResource` 的ID统一为数值：脚本(`id=1`)、字体(`id=2`)、样式(`id=3`)，引用分别更新为 `ExtResource("1")`、`ExtResource("2")`、`ExtResource("3")`，避免 `!int_resources.has(id)` 类型不匹配问题。
 - `card_style.tres` 资源类型为 `StyleBoxFlat`，场景中对应的 `ext_resource type` 也统一为 `StyleBoxFlat`，确保类型匹配。
 - 所有场景注释统一使用分号`;`作为注释前缀，避免 `#` 引发解析错误。
 - 脚本缩进规范化：main.gd 统一使用制表符（Tab）缩进，消除混合缩进导致的解析错误（报错行 246、264~291）。
- 布局更新：事件槽具体位置调整为顶端红框区域，依次为 `(1100,80)-(1220,230)`, `(1250,80)-(1370,230)`, `(1400,80)-(1520,230)`, `(1550,80)-(1670,230)`，手动等距、边距与间距一致；`create_leader_name_panels()` 改为固定坐标数组（左4: `(40,160/250/340/430)`；上2: `(540,80/720,80)`；右4: `(1540,160/250/340/430)`），不使用公式计算；名称为“阿卜杜勒-迈吉德一世”时字号设为 `14` 以保证完全容纳。
 - 布局更新：事件槽具体位置调整为顶端红框区域，依次为 `(1100,80)-(1220,230)`, `(1250,80)-(1370,230)`, `(1400,80)-(1520,230)`, `(1550,80)-(1670,230)`，手动等距、边距与间距一致；`create_leader_name_panels()` 固定坐标数组更新为：上2 整体左移为 `(410,80)` 与 `(950,80)`；右4 `x=1540, y=310/510/710/910`（间距 `x=200`，首张在事件槽下方且与其下边缘间距约 `80>0.35x`，末张到底边间距约 `130`）；左4 `x=40, y=310/510/710/910` 与右列对齐。不使用公式计算；名称为“阿卜杜勒-迈吉德一世”字号设为 `14` 以保证完全容纳。

## 系统实现逻辑

### 项目配置
- 项目名称: 帝国：博弈
- 主场景: res://main.tscn
- 使用特性: Godot 4.4, Forward Plus渲染器
- 项目图标: res://icon.svg

### 主场景逻辑
主场景实现了新的游戏界面：
1. 背景和顶部标题（统一字体）
2. 右上角三层面板（主牌堆/事件牌堆/弃牌堆），尺寸统一 120×150
3. 顶端横向四个事件槽（透明背景白边框），尺寸统一 120×150
4. 左4、上2、右4的领袖名称彩色矩形布局（十六进制颜色解析，名称居中，左/右垂直距离加大、上方两名下移且水平距离加大）

## 数据结构

### 卡牌数据结构
游戏数据结构采用统一字典：
`{ id: String, cardName: String, source: String, meta: Dictionary }`

类型来源：
- default（来自 default.json，每种2张）
- policy（来自 policies.json）
- event（来自 events.json）

计数显示：
- 牌堆：`main_deck.size()`
- 事件牌堆：`event_deck.size()`
- 弃牌堆：`discard_pile.size()`

## 建议

1. **扩展抽牌/弃牌逻辑**：补充抽牌接口与事件槽放置接口。
2. **完善UI/UX**：为面板与名称矩形添加过渡与高亮效果。
3. **数据验证**：确保JSON结构一致性并加入错误提示。
4. **资源ID规范**：所有 `SubResource` 建议统一使用数值ID，避免解析兼容性问题。
